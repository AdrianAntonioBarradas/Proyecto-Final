const express = require("express");
const mysql =require("mysql");

const app= express();

app.use(express.json());

var conexion = mysql.createConnection({
    host: 'localhost',
    database: 'catalogo',
    user: 'root',
    password: 'AaBc1234'
    });

app.post('/register', (req, res) => {
    conexion.query("INSERT INTO usuarios (idusuario, usuario,contrasenia,nombre,tipoUsuario) VALUES(?,?,?,?)",   [id,user,password,nomen,userType],(error, result)=>{
        console.log(error);
    });
});